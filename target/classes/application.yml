spring:
    application:
        name: newworld
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        type: com.zaxxer.hikari.HikariDataSource
        url: jdbc:mysql://127.0.0.1:3306/zyl?useUnicode=true&characterEncoding=utf8
        username: root
        password: Zyl123456
        hikari:
            minimum-idle: 2
            maximum-pool-size: 2
mybatis:
    mapper-locations: classpath*:mapper/**/*.xml
    configuration:
        map-underscore-to-camel-case: true
server:
    port: 6001
logging:
    level:
        root: info
    file:
        name: ${spring.application.name}_${server.port}.log
        path: ../logs
